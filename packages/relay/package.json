{
  "name": "@nightpermit/relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "npm run build:dependencies && tsc -p tsconfig.build.json",
    "build:dependencies": "npm run build --workspace=@nightpermit/permit && npm run build --workspace=@nightpermit/midnight-contract",
    "check": "npm run build:dependencies && tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js",
    "test": "npm run build:dependencies && vitest run"
  },
  "dependencies": {
    "@midnight-ntwrk/midnight-js-indexer-public-data-provider": "4.1.1",
    "@midnight-ntwrk/midnight-js-types": "4.1.1",
    "@nightpermit/midnight-contract": "0.1.0",
    "@nightpermit/permit": "0.1.0",
    "rxjs": "7.8.2"
  }
}
